# Temperature-Sensor-System
A C# console-based temperature monitoring system using events and delegates. The project simulates a temperature sensor that notifies connected devices—such as an alarm and a display screen—whenever the temperature changes. Demonstrates the observer pattern, OOP concepts like inheritance, encapsulation, and event handling.
